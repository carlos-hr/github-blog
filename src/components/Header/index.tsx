import { LogoIcon } from "../../assets/icons/logo";
import { HeaderContainer } from "./styled";

const Header = () => {
  return (
    <HeaderContainer>
      <LogoIcon />
    </HeaderContainer>
  );
};

export default Header;
